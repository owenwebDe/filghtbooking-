# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30

# MongoDB Configuration
MONGODB_URL=mongodb://localhost:27017
MONGODB_DATABASE=flightbooking

# Stripe Configuration
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Flight API Configuration (TravelNext)
FLIGHT_API_USER_ID=your_flight_api_user_id
FLIGHT_API_PASSWORD=your_flight_api_password
FLIGHT_API_ACCESS=Test
FLIGHT_API_IP_ADDRESS=127.0.0.1

# CORS Configuration
FRONTEND_URL=http://localhost:3000
ADMIN_URL=http://localhost:3001

# Firebase (only needed if using service account for enhanced features)
# Set GOOGLE_APPLICATION_CREDENTIALS environment variable to path of service account JSON file
# Or leave empty to use Firebase client SDK authentication